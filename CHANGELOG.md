# Improvement Changelog

| Stage | What you tried and why | Evidence | Decision/Learning |
|---|---|---|---|
| Baseline | Single prompt, no profile, no real spaced repetition | 16.62s avg per task, 0 real calendar events, no verification | Established the starting point |
| Iteration 1 | Structured student profile, carried through the whole pipeline | Profile correctly extracted in all 11 test cases, including a Portuguese-language case (test_parse_profile_works_with_portuguese_input) | Kept |
| Iteration 2 | Real spaced repetition (.ics), instead of dates suggested in loose text | 160 real calendar events generated across 11 cases vs. 0 for baseline | Kept — structural improvement, not incremental |
| Iteration 3 | Verification added after observing the risk of forced contextualization | 6/11 (54.5%) pass rate — caught real issues (forced contextualization, conceptual errors) in ~45% of cases, including a nonsensical "more musicians = faster album recording" analogy for inverse proportion | Kept — the failure rate confirms the risk was real, not hypothetical |
| Final | Full pipeline: 28.78s avg per task vs. 45min estimated manual effort (~94x faster); difficult case (unstructured syllabus) still approved, generating 15 correct events | evaluate.py full run, 11 cases | Main contribution: verification is the load-bearing capability — without it, ~45% of outputs would ship with an uncaught error |
| Bugfix (live) | Fixed `_parse_json_response` crashing when Gemini 3.6 Flash returns `.content` as a list of blocks instead of a plain string (`AttributeError: 'list' object has no attribute 'strip'`) | 12/12 tests passing after fix, including new regression test `test_extract_topics_handles_list_content_response` reproducing the exact shape that crashed live | Kept — normalizing LLM output shape at a single boundary function is safer than assuming a fixed shape throughout the pipeline; see Hot Take for the full lesson |
| Iteration 4 | Closed the self-correction loop identified in the Hot Take: added a conditional edge (`add_conditional_edges`) so `validate` routes back to `generate_flashcards` with the specific rejection reason when issues are found, capped at 2 total attempts via a new `retry_count` field in `AgentState`, instead of only flagging problems to the student | 19/19 tests passing (2 new full-graph integration tests). Real `evaluate.py` run: pass rate went from 6/11 (54.5%) before the retry loop to **10/11 (90.9%)** after — a +36.4 point improvement. Trade-off: average time per task roughly doubled (28.78s → 58.70s), since cases needing correction now do the generation+validation work twice | Kept — the pass-rate gain is large and the time cost is proportionate and expected (only failing cases pay the retry cost); 1/11 cases still fails even after 2 attempts, which is now the clearest remaining limitation, not a hidden one |
| Iteration 5 | Two content-integrity changes. (a) **Study guide:** `generate_flashcards_node` now also returns `topic_explanations` (`Dict[str, str]`, one 3-5 sentence overview per topic), shown before the deck in the Streamlit app and in the offline HTML — a fixed introduction, distinct from a CONCEPT card. (b) **Withhold unfixed cards:** `ValidationIssue` gains `affected_card_front`; after the retry loop gives up, `respond_node` drops the specific flashcards verification tied to an unfixed `conceptual_error` / `forced_contextualization`, keeps the topic's other verified cards, and adds one conservative SUMMARY placeholder only if a topic would otherwise be empty. `issues_found` is untouched (still surfaced for technical review); the offending content never reaches the student as reliable | 32/32 tests passing (8 new: topic-explanation generation + defaults + prompt; `affected_card_front` normalization; card withheld; siblings kept; no-op when approved / when issue names no card; fallback card when a topic is emptied). `evaluate.py` not re-run this pass (left for a manual run to avoid spending Gemini quota) | Kept — veracity over completeness: a wrong card is worse than a missing one. Third Hot Take example (playlist-as-50, audio-export-as-strict-proportion) now documents a risk the pipeline closes rather than just reports |
| Iteration 5.1 | Closed a gap in `_filter_unresolved_flashcards`: an unfixed `conceptual_error` / `forced_contextualization` whose `affected_card_front` the reviewer could not fill (or filled with text matching no real card) used to leave that card in place. `ValidationIssue` gains `affected_topic` (`validate_node` now asks for it — the topic is shown in `[brackets]` on every reviewed card, so it is always available); when an issue cannot be pinned to one card, the **whole affected topic** is withheld instead. Last resort, when even the topic is missing: every topic that still has cards is emptied (nothing unverified ships). Topics with no issue are untouched | 33/33 tests passing (`test_respond_keeps_cards_when_issue_not_tied_to_a_card` replaced by `test_respond_withholds_whole_topic_when_issue_names_topic_but_not_card` + `test_respond_withholds_all_topics_when_content_issue_has_no_anchor`; `test_validate_asks_which_card_and_normalizes_affected_card_front` extended for `affected_topic`; precedence of `affected_card_front` over `affected_topic` asserted). `evaluate.py` not run | Kept — the earlier "leave the ambiguous card in place" behaviour contradicted the veracity-over-completeness goal that motivated the whole feature |
| Iteration 6 | Architecture optimization: `parse_profile` and `extract_topics` have no data dependency (one reads `questionnaire_answers`, the other `syllabus_text` / `free_form_goal`), so `graph.py` now fans them out from `START` to run concurrently; `build_schedule` fans back in, waiting for BOTH (it needs `topics` **and** `student_profile.study_pace` — the "build_schedule doesn't depend on the profile" assumption was wrong, `nodes.py:245`). From `build_schedule` on the pipeline stays sequential and the retry loop is byte-identical. Writes are disjoint (`student_profile` vs `topics`/`topics_source`), so no reducer was needed. The two integration tests' positional `side_effect` lists were replaced with a prompt-content dispatcher, since the two fan-out LLM calls now arrive in unspecified order | 33/33 tests passing, stable across 5 consecutive runs (no flakiness). One instrumented real run: `parse_profile` (2.4s) and `extract_topics` (35.3s) measured running **concurrently** (full overlap of the shorter call); wall clock 94.0s vs ~96.4s serial. Saving = the shorter of the two calls off the critical path — 2.4s in this sample; the % is noisy here because this run hit large Gemini latency outliers (`extract_topics` 35s, `validate` 49s) | Kept — real structural parallelism (measured overlap), zero behaviour change; the win scales with how close `parse_profile` and `extract_topics` runtimes are |
| Doc & consistency pass | Full review of README/CLAUDE.md against the code, plus low-risk code cleanup. README fixes: Hot Take now describes the retry loop as shipped (was still "the clear next iteration"); "5/11 cases flagged" corrected from "6/11" (6/11 is the pass rate, not the failure count); a note explains the two real baseline times (16.62s pre-retry-loop run vs 26.64s most-recent run — Gemini API latency variance, not a typo); architecture diagrams in README **and** CLAUDE.md now show the conditional `validate → generate_flashcards` retry edge; README changelog copy re-synced with this file (missing "Bugfix (live)" row added); `invalid_card_type` documented in the Verification section and in `ValidationIssue`'s docstring; `GET /health` added to the endpoint list; flashcard-viewer button relabelled "🔄 Flip" (was "Show Answer") and the jump mini-board documented. Code: tuning constants (`TOPIC_SPACING_DAYS`, `CARDS_PER_TOPIC`, `CARD_TYPES`) regrouped at the top of `nodes.py`; dict access in `respond_node`/`validate_node` standardised on `.get()`; card-type→emoji map unified into one `TYPE_ICONS` in `state.py`, imported by both `nodes.py` and `streamlit_app.py`; dead `card_index`/`card_flipped` session state removed from `streamlit_app.py`; Streamlit subtitle qualified with "once downloaded" so it no longer implies generation itself works offline | 24/24 tests passing after every individual change (backend only — `streamlit_app.py` has no automated coverage; verified by `py_compile` + import-resolution check). No behaviour change intended or observed | Kept — pure documentation/consistency and mechanical cleanup; deferred (not done): dedup of the `_parse_json_response` flatten logic, the non-functional `fade-in-section` wrapper, `render_result` running once per tab, extracting the ~185-line deck-HTML builder to its own file, and minor Streamlit UX items (date `min_value`, surfacing `retry_count`, deck interaction hint) |

| Iteration 7 | (a) `CARDS_PER_TOPIC` raised `{light:2, moderate:3, intensive:4}` → `{3, 4, 5}` — the veracity filter (Iteration 5) can drop flagged cards, so a low base budget could leave a topic thin; a more generous budget keeps enough verified material after filtering. (b) New `Flashcard.used_interest` field + a `🎯 personalized using: <interest>` tag on the deck, kept only when the interest actually appears in the profile (`_match_interest`) — the visible, checkable form of "personalization is the product". (c) `_parse_json_response` now makes one defensive repair-and-retry pass on `JSONDecodeError`, tolerating a trailing comma before `}` / `]` (the exact shape that crashed a re-run of `evaluate.py`); a genuinely truncated object still raises. `evaluate.py` also now sums `removed_flashcards` across the 11 cases (new "Flashcards delivered / withheld" line). | 44/44 tests passing (11 new since the 33/33 checkpoint: `used_interest` capture / default / honesty-guard / prompt; four `tests/test_streamlit_deck.py` cases lifting `build_flashcard_deck_html` via `ast` + `exec`; trailing-comma tolerance in / out of a fenced block + still-raises-on-truncation). **Full `evaluate.py` re-run did not complete across three attempts** — one malformed-JSON response (now handled), two transient `httpx.ReadError [WinError 10053]` on the first request — so the metrics table stays at the Iteration 4 numbers with a freshness note. | Kept — the pass-rate mechanism (`approved` set by `validate_node`) is unchanged; the table's time figure is now an upper bound (parallelization shortens the critical path); the withheld-cards metric is instrumented and pending a clean run. |
| Engineering discipline (ongoing) | JavaScript generated inside a Python f-string (the Streamlit flashcard deck) is a footgun — twice it shipped broken because an f-string ate a `\n`/backtick and blanked the deck with no stack trace. Rule adopted: every generated JS block is extracted and run through `node --check` before it's considered done, and `tests/test_streamlit_deck.py` lifts `build_flashcard_deck_html` out with `ast` + `exec` (no Streamlit import) so the rendered deck output is under automated test. | The two live breakages, then zero since the gate; deck JS `node --check` rc 0 on every change | Kept — cheap mechanical gate for a bug class that has no runtime error |
| Flashcard type icons | Swapped the deck's five type emoji (💡🎯⚠️❓📝) for inline outline SVGs in a new `TYPE_ICONS_SVG` map in `streamlit_app.py` (deck only — `state.py`'s emoji `TYPE_ICONS` still feeds the generated offline HTML). `stroke="currentColor"`, viewBox `0 0 24 24`, 16–18px, no fill/shadow, fully embedded (works offline). Labels switched from `innerText` to `innerHTML` (SVG raw, text through `esc()`). | 46/46 tests passing (2 new in `test_streamlit_deck.py`: inline stroke SVGs present / not emoji, and unknown type falls back to the QA icon). Deck `<script>` `node --check` rc 0. | Kept — purely a visual/icon-style swap, no logic change |
| Iteration 8 | `MAX_FLASHCARD_RETRIES` raised 2 → 3 — one more self-correction pass before the graph gives up and `respond_node` withholds the still-flagged card. One-line config change in `nodes.py`; the give-up integration test now derives its worst-case LLM call count from the constant (`2 + 2 * MAX_FLASHCARD_RETRIES` = 8) so it tracks future changes to the budget. Doc mentions of "2 attempts" / "MAX_FLASHCARD_RETRIES = 2" updated in README, CLAUDE.md and the Streamlit architecture diagram/caption; the Iteration 4 changelog row is left as the historical record of what that iteration did. | 46/46 tests passing. `evaluate.py` not re-run — configuration change, no result claim (the pass-rate mechanism and the withholding behaviour are unchanged). | Kept — an extra attempt at fixing a flagged analogy costs at most one more LLM round-trip, and only on cases that were already failing verification |

<!-- Add new rows as development progresses -->
